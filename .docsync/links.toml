# docsync links - code-documentation relationships

# CLI module → CLI reference + index quick start
[[link]]
code = "src/docsync/cli.py"
docs = ["docs/cli/reference.md", "docs/index.md#Quick Start"]

# Configuration → configuration docs
[[link]]
code = "src/docsync/config.py"
docs = ["docs/configuration.md#pyproject.toml"]

# Section parsing → configuration (staleness detection section)
[[link]]
code = "src/docsync/sections.py"
docs = ["docs/configuration.md#Staleness Detection"]

# Staleness logic → configuration
[[link]]
code = "src/docsync/staleness.py"
docs = ["docs/configuration.md#Staleness Detection"]

# TOML links → configuration
[[link]]
code = "src/docsync/toml_links.py"
docs = ["docs/configuration.md#links.toml"]

# Hook → getting started + index
[[link]]
code = "src/docsync/hook.py"
docs = ["docs/getting-started.md", "docs/index.md#Track Doc Sync"]

# Donttouch → configuration + index
[[link]]
code = "src/docsync/donttouch.py"
docs = ["docs/configuration.md#donttouch", "docs/index.md#Flag Protected Content Changes"]

# Coverage → configuration
[[link]]
code = "src/docsync/coverage.py"
docs = ["docs/configuration.md#Validation and Coverage"]

# Bootstrap → configuration + getting started
[[link]]
code = "src/docsync/bootstrap.py"
docs = ["docs/configuration.md#Bootstrap", "docs/getting-started.md#Step 6: Bootstrap Additional Links"]

# Validation → configuration
[[link]]
code = "src/docsync/validation.py"
docs = ["docs/configuration.md#Validation and Coverage"]
